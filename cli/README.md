# Inovix Terminal Security Dashboard

Run from the repository root:

```powershell
python -m cli.main
```

Keys: D Dashboard, T Threats, N Network, P Processes, I Intelligence,
E Events, S Scan, L Logs, C Config, Q Quit.

The UI consumes the existing security-engine and live system telemetry.
