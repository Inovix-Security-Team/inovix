# Machine Learning

This directory is reserved for future machine-learning components.

The initial Inovix Security Engine uses deterministic rule-based analysis.

Future ML components should implement interfaces compatible with the
existing analyzer/detector architecture without changing the engine's
core orchestration.